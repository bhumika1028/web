<?php
// Initialize sum to 0
$sum = 0;

// Loop from 0 to 30
for($x = 0; $x <= 30; $x++) {
    $sum += $x; // Add current number to the sum
}

// Display the total sum
echo "The sum of the numbers 0 to 30 is $sum" . "\n";
?>
